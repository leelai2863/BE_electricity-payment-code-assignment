# Sao chép thành .env.prod trên server (KHÔNG commit file chứa secret thật).
#
# Bắt buộc:
CORS_ORIGIN=https://domain-frontend-cua-ban

# Tuỳ chọn (mặc định trong docker-compose.prod.yml đã set qua biến compose):
# OPENAI_API_KEY=
# OPENAI_VISION_MODEL=gpt-4o-mini
# OPENAI_TEXT_MODEL=gpt-4o-mini
# BACKEND_HOST_PORT=3001
